export enum EButtonTypes {
  Contained = 'contained',
  Text = 'text',
  Tab = 'tab',
}

export enum ETabs {
  Focus = 'Focus',
  ShortBreak = 'Short Break',
  LongBreak = 'Long Break',
}

export enum EStatus {
  Idle = 'idle',
  Running = 'running',
  Paused = 'paused',
  Finished = 'finished',
}
